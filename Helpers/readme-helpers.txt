//
// Graal’s join("PlayerStatus") = in Swift:
//
// class PlayerManager: ObservableObject {
//    @Published var level: Int = 1
//    @Published var health: Int = 100
//}
//
//Use @EnvironmentObject to inject that across views without cluttering each one.
