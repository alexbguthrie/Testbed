import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("⚙️ Settings")
                .font(.largeTitle)
            Text("Manage your preferences here.")
        }
        .padding()
    }
}
