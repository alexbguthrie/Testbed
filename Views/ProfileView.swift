import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("👤 Profile")
                .font(.largeTitle)
            Text("This is your profile page.")
        }
        .padding()
    }
}
