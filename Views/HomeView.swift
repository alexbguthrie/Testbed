import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("🏠 Home")
                .font(.largeTitle)
            Text("Welcome to Testbed!")
        }
        .padding()
    }
}
